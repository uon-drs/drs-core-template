# CI — Backend (.NET API)
#
# Triggers on PRs and pushes to main that touch the backend directory.
# Builds, tests (with coverage), and publishes a deployment artifact.
#
# Artifact produced: 'api-drop' — used by cd-backend.yml
#
# Starts an Azurite container so integration tests can exercise the Azure Storage
# account (see infra/modules/components/storage-account.bicep) the same way local
# dev does via docker-compose.yml. Remove this step if the backend has no tests
# that touch Blob/Queue/Table storage.

trigger:
  branches:
    include:
      - main
      - feature/*
      - bugfix/*
  paths:
    include:
      - backend/**

pr:
  branches:
    include:
      - main
  paths:
    include:
      - backend/**

variables:
  - template: variables/common.yml

pool:
  vmImage: 'ubuntu-latest'

stages:
  - stage: Build
    displayName: 'Build & Test .NET API'
    jobs:
      - job: BuildAndTest
        displayName: 'Build, Test, Publish'
        steps:
          - script: |
              docker run --detach \
                --publish 10000:10000 \
                --publish 10001:10001 \
                --publish 10002:10002 \
                mcr.microsoft.com/azure-storage/azurite \
                azurite --skipApiVersionCheck \
                        --blobHost 0.0.0.0 \
                        --queueHost 0.0.0.0 \
                        --tableHost 0.0.0.0
            displayName: 'Start Azurite'

          - task: UseDotNet@2
            displayName: 'Install .NET $(dotnetVersion)'
            inputs:
              version: $(dotnetVersion)
              performMultiLevelLookup: true

          - task: DotNetCoreCLI@2
            displayName: 'Restore NuGet packages'
            inputs:
              command: restore
              projects: $(backendSolutionPath)

          - task: DotNetCoreCLI@2
            displayName: 'Build solution'
            inputs:
              command: build
              projects: $(backendSolutionPath)
              arguments: '--configuration $(buildConfiguration) --no-restore'

          - task: DotNetCoreCLI@2
            displayName: 'Run tests with coverage'
            inputs:
              command: test
              projects: $(backendTestPath)
              arguments: >
                --configuration $(buildConfiguration)
                --no-build
                --collect:"XPlat Code Coverage"
                --results-directory $(Agent.TempDirectory)/coverage

          - task: PublishCodeCoverageResults@2
            displayName: 'Publish code coverage'
            inputs:
              summaryFileLocation: '$(Agent.TempDirectory)/coverage/**/coverage.cobertura.xml'
              pathToSources: '$(Build.SourcesDirectory)/backend'

          - task: DotNetCoreCLI@2
            displayName: 'Publish application'
            inputs:
              command: publish
              projects: $(backendProjectPath)
              arguments: >
                --configuration $(buildConfiguration)
                --no-build
                --output $(Build.ArtifactStagingDirectory)/api
              publishWebProjects: false
              zipAfterPublish: true

          - task: PublishPipelineArtifact@1
            displayName: 'Publish artifact: api-drop'
            inputs:
              targetPath: '$(Build.ArtifactStagingDirectory)/api'
              artifactName: 'api-drop'
              publishLocation: 'pipeline'
